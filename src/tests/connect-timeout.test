#!/bin/sh
# Modified for Goblin Skiff on 2026-09-19.
set -eu
. "$(dirname "$0")/e2e-test-subrs"
set_locale || exit 77
command -v python3 >/dev/null 2>&1 || exit 77
exec python3 "${srcdir:-.}/connect-timeout.py"
